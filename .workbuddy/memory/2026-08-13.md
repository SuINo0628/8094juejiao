# 2026-08-13 工作日志

- 8094juejiao 互动页(index.html) 新增点击音效：用 Web Audio API 合成，不依赖任何音频文件（分享包不丢音）。音效类型 enter/tap/good/bad/win/alert/hit/wer；右上角加静音开关 muteBtn（SVG 喇叭图标，默认开）。
- 新增角色「玖零尔斯·菜市场自提」(jiulingersi.webp)：用 rembg AI 抠图（拿双麦唱歌小浣熊），绝交文案，暖棕色 #D4A574。
- 「好啦好啦，别点了！」弹窗改为随机触发：时间>30s 后每次点击有 18% 概率弹出（不再固定 120s）。
- 月神星倒计时从 20s 加长到 35s，更有存在感。
- 新增角色「美宝」(meibao.webp)：用 rembg AI 抠图（蹦跳小鹿），绝交文案，鹿棕色 #C4A882。
- new role 3471 (3471.webp): rembg BG remove (raccoon), custom kind=xiehuang dialog. text: 你今天蟹黄了吗？ options: 蟹黄了(+1) / 没有蟹黄(+3), float text 蟹黄+N + confetti, color #9E8B7D.
- 玖零尔斯(jiulingersi) 改新交互 kind=work: 文案"你想工作多久？"，起始 5000s 倒计时。选项"不想工作"→随机 工作时间-100/-50/-500/-10；"想工作"→弹窗"我没允许你工作"；倒计时结束→弹窗"工作做完了"。点图随机飘字 工作时间-100/+100/-600/-888/+888。
- 修复彩带偶发特大条：移除 canvas-confetti 的 ribbon 形状（每个粒子随机缩放导致偶尔出现超大横幅），彩带改由尺寸固定的 HTML .ribbon/.ribbon-rest 承担；落定彩带数量 3->5 条补偿视觉。
- 头像点击攻击飘字：仅蚕豆豆(candoudou)改为「被攻击 豆豆+N」(N=1~10随机)，其余角色维持「被攻击！+120秒」不变。
- 玖零尔斯图片(jiulingersi.webp)按用户要求"换回去"：用原截图重新跑最初抠图命令，恢复为最初版(681x877, 55KB)。下方轻微残留与最初版一致（用户已知）。待用户决定是否需折中清理。
- 蛋黄水母(danhuangshuimu) 补音效：其选项点击本就有 tap(经fillOpts)，但头像点击此前被排除在 setupImageHit 外(静默无反应)。新增 setupLoveHit() 给头像加点击->飘爱心(floatHearts)+good 柔和音效(love主题不攻击)。
- 彩带彻底禁用：spawnOneRibbon() 和 spawnRestRibbons() 均改为空函数(no-op)，消除偶发大长条。canvas-confetti 的 square/circle 小亮点保留（尺寸恒定不会变大）。调用点(5+6处)保留不动，只是不再产生 DOM 元素。
- 新增角色「高塔」(gaota.webp)：用 rembg AI 抠图（小水獭），默认绝交文案(breakup)，水獭灰蓝 #7A9B9E。
- 新增角色「沉聿」(chenyu.webp)：用 rembg AI 抠图（黑猫），绝交文案但名字改为「郑函泽要和你绝交」（非默认崔胜澈8094），纯黑 #2D2D2D。
