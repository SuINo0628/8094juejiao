# 2026-08-11

## 8094juejiao「你是谁」网页（GitHub Pages, 仓库 SuINo0628/8094juejiao）
- 修复图片不显示：index.html 里 15 个角色头像误写成 .webp，实际文件是 .png/.jpg。已逐一改回真实扩展名并推送。
- 替换头像图片（直接覆盖同名文件后 commit+push）：
  - ISTP.jpg → 冰淇淋小狗（commit 90386ed）
  - 9194.png → 小浣熊（commit d848659）
- 新增 9194 点击被攻击效果：点头像→抖动+红光闪+飘「被攻击！」（在 characters 加 hitOnClick:true，新增 setupImageHit()，CSS 加 .img-hit / @keyframes imgHit）。commit 841fb13 已推送。
- 部署方式：改完 git commit + git push 到 main，GitHub Pages 约 1–2 分钟重建后刷新链接生效。git push 偶发静默失败，重试即可。
- 路人橘(lurenju)被攻击飘字改随机「掉落橘子+1/+5/+10」或「+1/+5/+10」：新增 randomOrangeText()，setupImageHit(id) 按 id==='lurenju' 区分文案。
- 被攻击特效升级：改为「除蛋黄水母(danhuangshuimu)外所有人」点头像触发（enter() 里 `if(id!=='danhuangshuimu') setupImageHit()`）；闪光用各自主题色 var(--accent)。点击同时若有倒计时则时间+120（ringNum 存在才加），飘「被攻击！+120秒」，到 120 弹「好啦好啦，别点了！」。commit 5339640 已推送。
- 约定：非 breakup/无倒计时角色点头像只播攻击动画、不+时间（无 ringNum）。
